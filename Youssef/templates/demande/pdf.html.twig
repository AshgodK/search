<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{{ demande.id }}_demande.pdf</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f0f0;
            margin: 0;
            padding: 0;
        }
        .container {
            width: 80%;
            margin: auto;
            background-color: #fff;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            padding: 20px;
            margin-top: 30px;
        }
        .header {
            text-align: center;
            margin-bottom: 20px;
        }
        .title {
            font-size: 24px;
            font-weight: bold;
            margin-bottom: 10px;
        }
        .details {
            margin-bottom: 20px;
        }
        .details p {
            margin: 5px 0;
        }
        .details p strong {
            font-weight: bold;
        }
    </style>
</head>
<body>
<div class="container">
    <div class="header">
        <h1 class="title">Demande Details</h1>
    </div>
    <div class="details">
        <p><strong>Description:</strong> {{ demande.description }}</p>
        {# Add user details if the relationship exists #}
        {% if demande.user is not null %}
            <p><strong>User:</strong> {{ demande.user.nom }}</p>
            <p><strong>Prénom:</strong> {{ demande.user.prenom }}</p>
            {# Add more user details if needed #}
        {% endif %}
        {# Add offer details if the relationship exists #}
        {% if demande.offre is not null %}
            <p><strong>Offer:</strong> {{ demande.offre.nom }}</p>
            {# Add more offer details if needed #}
        {% endif %}
    </div>
    {# Add more sections or details of the request as needed #}
</div>
</body>
</html>
