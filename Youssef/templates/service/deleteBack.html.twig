<form method="post" action="{{ path('app_service_deleteB', {'id': service.id}) }}">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ service.id) }}">
    <button class="btn">Delete</button>
</form>
